#!/usr/bin/env bash

set -e

cd "$(dirname "$0")/.."

# Python deps (HA + test + lint tooling)
python3 -m pip install --upgrade pip
python3 -m pip install --requirement requirements_dev.txt

# Card deps
cd card
npm install

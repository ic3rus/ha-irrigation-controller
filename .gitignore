# --- Secrets / config sensible ---
secrets.yaml
*.pem
*.key
.env
.env.*

# --- Claude Code --- + Bmad
.claude/*
_bmad*
_bmad-output/*


# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
uv.lock
.coverage
htmlcov/
dist/
build/

# --- Node / card build ---
node_modules/
# Screenshots of the card's visual harness (`npm run visual` in card/): looked at, never committed.
card/visual/out/
# NOTE: the built card bundle in custom_components/ha_irrigation_controller/frontend/
# is COMMITTED on purpose (HACS ships it) — do not ignore it.

# --- Home Assistant dev instance (scripts/develop) ---
# Everything HA generates at runtime is ignored; configuration.yaml is committed
# on purpose so a fresh clone gets a working dev instance with no setup step.
config/*
!config/configuration.yaml

# --- Home Assistant / ESPHome (artefacts locaux) ---
.esphome/
*.db
*.db-shm
*.db-wal
*.log

# --- Éditeurs / OS ---
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

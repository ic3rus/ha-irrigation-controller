-r requirements_lint.txt
-r requirements_test.txt
# homeassistant is pinned in requirements_test.txt — the single place it belongs,
# so the dev venv and the CI stable leg can never drift apart again.
colorlog==6.12.0
